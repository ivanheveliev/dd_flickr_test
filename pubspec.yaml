name: dd_flickr_test
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.2
  flutter_pagewise: ^2.0.1
  http: ^0.13.4
  flutter_bloc: ^7.0.1
  intl: ^0.17.0
  shared_preferences: ^2.0.15
  firebase_analytics: ^9.1.9
  firebase_core: ^1.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  fonts:
    - family: Emotions
      fonts:
        - asset: fonts/Emotions.ttf

  assets:
    - assets/